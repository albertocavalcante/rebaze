load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "rebaze-bazel",
    srcs = glob(["src/**/*.rs"]),
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        "//crates/rebaze-gradle",
        "@crates//:anyhow",
        "@crates//:serde",
        "@crates//:thiserror",
        "@crates//:tracing",
    ],
)
